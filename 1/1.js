(() => {
  const classes = {
    layerChart: "layerChart",
  };

  function main() {
    const layerChart = window.document.getElementsByClassName(
      classes.layerChart
    )[0];
  }

  main();
})();
